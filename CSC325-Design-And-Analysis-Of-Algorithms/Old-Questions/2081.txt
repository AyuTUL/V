1.
dynamic programming				memoization
-bottom up approach				top down
-solves all possible sub problems first and	starts wit poriginal problem and recurse down to
combines them for bigger problems		sub problems
-implemented using iteration to fill up		uses recursion by keeoing trakc of sub problems
n dimnesional table				in cache/look up table
-better poerformance due to no ovehead for	more overhead due to recursive function calls
recursion					and stack management
-all sub problems have to be solved at least	solves only those sub problems defnintely
once						needed for final solution
2.
-in quick sort, array is partionened about an elemtn called pivot
-partiointing step rearranges th earray so that pivot elemnt is placed in its coorect position
-all elemt sto left of pivot are smaller and to the right are greater
-time complexity is O(n^2) which occurs when partioining process is highly unbalanced ie
input is nearly/already sorted or reverse sorted
-randomized quick sort handles this by choosing a pivot randomly from array isntead of 1sdt/last
element which makes worst case scenario highly unlikely ensuring average case time complexity
of O(nlogn)


algorithm:
-take 1st element as pivot
-set L at start of array and R at end of array
-while L<R:
	-increment L till element at index position <= pivot
	-decrement R till element at index position > pivot
	-if L<R:
		-swap a[L] and a[R]
-swap pivot and a[R]
-repeat 1 to 4 in left are right sub arrays until entire array is sorted

3.
-optimization problem is problem of finding best solution(min/max) rfom all fesible soluition
-greedy algorithm obtains solution for optimzastion problem
-makes choice that seems best at that moment
-no, greddy algorithm desotn always guartantee optimal soln for evey problem although it is
designed to find bset solution among fesible options in optimziation problems
-optimal soln only if algorithm has 2 properties:
	greedy choice:globally optimal soln can be reached by creating locally optimal soln
	optimal substructure: optimal sol cointains optimal soln to sub problems

knapsack problem:
-set of itesm, each with weight and value
-determine subset of itesm to include in collection so that toal weight <=limit and total
value is as large aspossible
-if either whole item is taken or left behind, 0-1 knapsack
-if fraction of item is taken, fractional knapsack
-O(n) + O(nlogn) + O(n) = O(nlogn)

algorithm:
n items,each with value Vi and weight Wi and knapsack capacity W
-calc per unit value of each item Vi/Wi
-sort items based on Vi/Wi in desc order
-while knapsack isnt full and item list isnt empty:
	-if W>=Wi, put whole item in knapsack
	-else put fraction of item that best fits remaining capacity of knapsack

5.
T(n)=a(n/b)+f(n)
I:n^logb(a) > f(n) => T(n)=theta(n^logb(a))
I:n^logb(a) = f(n) => T(n)=theta(n^logb(a)*log2(n))
I:n^logb(a) < f(n) => T(n)=theta(f(n))

6.
algorithm:
-set f1=0, f2=1
-input nth term of fibonacci sequence as n
-set i=3
-while(i<=n)
	-fn=f1+f2
	-f1=f2
	-f2=fn
	-i++
-print fn as nth term of sequence

time complexity=O(n), loop executes at most n-2 times
space complexity=O(1), 5 var

12. a.
asymptotic nmtation is standard notaiton used to analyze complexity of algorithm in terms of
time/space

big Oh:
-given 2 non ngeative valued functions f and g, the function f(n) is said to be big oh of function
g(n) written as f(n)=O(g(n)) if there exists some positive constant c and n0 such that f(n)<=
c.g(n) for all n>=n0.
-provides asymptotic upper bound on f(n)

big omega:
-given 2 non ngeative valued functions f and g, the function f(n) is said to be big omega of 
function g(n) written as f(n)=Omega(g(n)) if there exists some positive constant c and n0 such
that f(n)>=c.g(n) for all n>=n0.
-provides asymptotic lower bound on f(n)

big theta:
-given 2 non ngeative valued functions f and g, the function f(n) is said to be big theta of 
function g(n) written as f(n)=Theta(g(n)) if there exists some positive constant c1 c2 n0 such
that c1.g(n)<=f(n)<=c2.g(n) for all n>=n0.
-provides asymptotic tight (both lower and upper) bound on f(n)
