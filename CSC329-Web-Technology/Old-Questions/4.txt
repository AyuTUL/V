JS:
-lightrweight scripting language embedded directly into HTML pages to add interactivity
-dynmic texet into html page using vairables
-react to evnets
-read nadwrite html elements
-data validation
-create cookies

function functionName (paramtetr....) {
	code
	optional reutrn value
}

object literal:
let object = {
	property: value,
	funntionName:function() {}
	}
objectName.functionName;

let person = {
	first: "cris",
	last: "ron",
	getname: function() {
		alert(person.first+' '+person.last);
	}
}
person.getname();

function constructor:
function Student(name,address) {
	this.name=name;
	this.address=address;
}
let student1=new Student('Bale','Wales');
document.write(student1.name);

class:
class Car {
	constructor(name,year) {
		this.name=name;
		this.year=year;
	}
	age () {
		const date = new Date();
		return date.getFullYear()-this.year;
	}
}
const myCar = new Car("Ford",2014);

regular expression:
-objectthat describes sequence of characters for defining preices serach pattern

regex literal:
cosnt regex=/abc/;

rgeexp constructor function:
cost regex=new RegExp(/^as$/);

HTML DOM:
-created when web page is loaded
-cosnbtructed as tree of objects

cookie:
-user ionformation stored in text file in computer

document.cookie="user=Ram";
var x=document.cookie;