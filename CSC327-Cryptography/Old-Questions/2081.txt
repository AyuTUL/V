1. IDEA internation data encryption algorthim 
-symmetric key block cipher by Lai, Massey, Murphy
-replacement fro DES

parameters:
-block size: 64 bit bloks of plaintext
-key size: 128 bit encryption key
-8 idetntical rounds followed by output transformation
-total 52 subkeys 16 bit loing each generated from initail 128 bit key

-mix oeprations from2 different mathematical grius that are incompatible which creates higly non
linear relationship between plaintext and ciphertext

bitwise xor-logical xor of 2 16bit words
addition modulo 2^16-addition of 16 bit integrs ignoring carry out of 16th bit
multiplication modulo 2^16 + 1-multiplcaotion fo 16 bit interers

sub key generation:
from 128 bit intial key, 52 sub keys z1-z52 are produced as:
-1st 8 subkeys z1-z8 are taken direclty from first 128 bits of key
-key is CLS by 25 bits and next 8 key sare extracted
-shifting and extraction repeates auntil all 52 subkeys are generated
-each of 8 rounds uses 6 subkeys 8x6=48 and final output transformation uses remaing 4

round operation:
-each round64 buit input is divided into 4 16bit sub blocks x1 x2 x3 x4
FIG

output transformation:
-4 subkets z49-z52 to produce 64 bit ciphertext Y1 Y2 Y3 Y4

2. MAC message authentication code FIG
-cryptographic checksum/tag generated by algorithm that uses secret key to eunsrie ingreity and 
authenticity of message
-only pareties with ke can generate/verify tag

MD4:
-cryptographic laogrithm that takes arbritaray length input and produces 128 bit message digest

append padded bits: FIG
-message in padded till ength is congrunent to 448 modulo 512

append length: FIG
-64 bit block that repsetsen ength of orignal mesage is appened

initialize md buffer:
-4 32bit register sABCD initliare as:
A: 01 23 45 67
B: 89 AB CD EF
C: FE DC BA 90
D: 76 54 32 10

process the blocks:
-3 rounds with 16 steps each

R1: F(X,Y,Z) = (X AND Y) OR (NOT X AND Z) 0-15
R2: G(X,Y,Z) = (X AND Y) OR (X AND Z) OR (Y AND Z)
R3: H(X,Y,Z) = X XOR Y XOR Z

output:
message digest is ABCD 

needham schroder: FIG
1 IDa || ID b || N1
2 E(Ka,[Ks || IDa || IDb || N1]) || E(Kb,[Ks || IDa])
3 E(Kb,[Ks || IDa])
4 E(Ks,N2)
5 E(Ks,f(N2))

-Ka and Kb are secret keys of ALice and Bob shared with KDC
1 alice sends Ida, Idb random number nonce N1 to KDC, N1 prevents use of old Bob key
2 KDC geenrates session key Ks and sends to alice ticket T for Bob
3 alice decrypts session key and sends ticket T to bob, mutual authentiation
4 bob decrypts T and challenges alice with nonce N2
-vulnerable to relay attacks

3. discrete lgorithm
-one way function, foundation of public key security for diffie hellman and elgamal
-y=g^x mod p is easy but finding xgiven y is hard for large primes

6. firewall
-firewall acts as barrier between trusted interal network and untrusted external network
to monitor and control flow of incoming/outgoing traffic between them

circuit level gateway				stateful inspection
-verifies eastblishment of tcp session,not	-anaylyuze packet contents based on connection
inspect packet contents				state
-session layer					-newtork and transport laywe
-faster byt less granular control		-slower but better security
-desno monitor packet payload			-monitor and filer based on payload and session
						info
7. intrusion and IDS
-unauthorized attempt to acces/manipulate system and its data for malicious purpoise
-breaching secirtu to access sensitive info,disrupt operation or install maliciouis sofwatre

-IDS is security tool for monitoring network/system for malicious actions/policy viulations

types:
statistical anomaly detection:
-collectino of data relating to behaviour of legetimate users over a period of time
-sus statistical test to observe behaviour to determien with high level of confindence if 
behaviour is legetimate or not

threshold detection:
-counting number of occurreces of specifided event type over interval of time

profile based detection:
-characterizng past behaviour of users and detect significant deviation

rule based detection:
-attempt to define ser of rules to decide given behaviour is of intrduer

anomaly deection:
-historical audit records are analyzed to idenityfy usage pattersn and auto generate rules
that describve these patterns

peenratrion identification:
-rules for identifying know penetraions or penetrations that would exploit known weakneses

9. dos
-deliberate attack for make serices unaavliable to autothrized users by exhasting bandwhith
or system resources

PKI public key infrastriucutre:
-fraweork for manging public keys and digital certifaicts
-how entities trust each other thought certificates issued by trusted CA

types:
single ca model: evreyoen trusts sungole CA
web of trust: uesrs mutulasy sign and trust each others keys
mesh model: muultiple CA cross certify each other to extend trust

10. modes of bloc kcipher: FIGS
ECD electronic code book
-plaintext is handles one block at aatime, each block is encrypted suing same key
-for given key, eeach plaitnext has uniqiue cihpertext

CBC cipher block chaining:
-input to encrption alkgorthi mis XOR of curernt P and preceding C
-same key is used for each block

CF cipher feedback mode:
-each C gets feedback into encryption  proces to encrypt next P
-data is encrypted in shift registers with key k taking S number of MSB

OF output feedback:
-output of encryptino is fed back into shift register

counter mode:
-counter equal to P and incremented by 1 for each block
-counter is encrypted with key LK and xored with output of P to produe C